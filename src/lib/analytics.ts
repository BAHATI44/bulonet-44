// Analytics stub - replace with real analytics provider
export const initAnalytics = (_config: { domain: string }) => {
  // Initialize analytics (e.g., Plausible, Umami, etc.)
};

export const trackPageView = (_path: string) => {
  // Track page view
};

export const trackEvent = (_name: string, _props?: Record<string, string>) => {
  // Track custom event
};
